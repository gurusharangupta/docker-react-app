sudo: required
services:
    - docker

before_install: 
    - docker build -t gurusharangupta/docker-react-app -f Dockerfile.dev .
    
script:
    - docker run -e CI=true gurusharangupta/docker-react-app npm run test